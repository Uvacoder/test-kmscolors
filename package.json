{
  "name": "ts-image-palette",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "update": "yarn update --save",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.8.4",
    "vite": "^3.1.8"
  }
}